// Roll-up build. SDK build configuration lives in each SDK's own build.gradle.kts
// (uisdk/, childsdk/, parentsdk/) — each is a standalone Gradle build composed
// here via `includeBuild` in settings.gradle.kts.
